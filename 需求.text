 需求：
 1.显式制定debug模式， 只有在要求进入debug模式时，才运行终端里的人机交互界面。
 2.要能把觉得好的llm heuristic 存档下来（在debug模式存档，存完档还可以继续进行交互而不是直接退出），并且必须人为命名，以供后续调用。
 3.所有的存档heuristic都存在 heuristics文件夹下，格式都是可以在非debug模式下直接调用的heuristic标准格式，而不是每次调用都要开交互界面。
 4.每一次打开debug模式，都要显式指定从哪个heuristic开始，应该可以从任意标准heuristic开始，也可以选择从0开始。
